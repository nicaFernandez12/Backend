<div class="row">
    <div class="col-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="display-4">
                <i class="fas fa-boxes"></i> Lista de Productos
            </h1>
            <div class="text-muted">
                <i class="fas fa-info-circle"></i> Vista estática
            </div>
        </div>
        
        {{#if error}}
            <div class="alert alert-danger" role="alert">
                <i class="fas fa-exclamation-triangle"></i> Error: {{error}}
            </div>
        {{/if}}
        
        {{#if hasProducts}}
            <div class="row mb-3">
                <div class="col-12">
                    <div class="alert alert-info" role="alert">
                        <i class="fas fa-lightbulb"></i> 
                        <strong>¿Quieres ver los productos actualizándose en tiempo real?</strong> 
                        <a href="/realtimeproducts" class="alert-link">Ir a la vista en tiempo real</a>
                    </div>
                </div>
            </div>
            
            <div class="row">
                {{#each products}}
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card product-card shadow-sm h-100">
                            <div class="card-header bg-primary text-white">
                                <h5 class="card-title mb-0">
                                    <i class="fas fa-tag"></i> {{this.title}}
                                </h5>
                            </div>
                            <div class="card-body">
                                <div class="row mb-3">
                                    <div class="col-8">
                                        <span class="badge bg-success price-badge">
                                            <i class="fas fa-dollar-sign"></i> ${{this.price}}
                                        </span>
                                    </div>
                                    <div class="col-4 text-end">
                                        <span class="badge bg-info stock-badge">
                                            <i class="fas fa-boxes"></i> {{this.stock}}
                                        </span>
                                    </div>
                                </div>
                                
                                <p class="card-text text-muted">
                                    <i class="fas fa-align-left"></i> {{this.description}}
                                </p>
                                
                                <div class="row mb-2">
                                    <div class="col-12">
                                        <small class="text-muted">
                                            <i class="fas fa-barcode"></i> <strong>Código:</strong> {{this.code}}
                                        </small>
                                    </div>
                                </div>
                                
                                {{#if this.category}}
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <small class="text-muted">
                                                <i class="fas fa-folder"></i> <strong>Categoría:</strong> {{this.category}}
                                            </small>
                                        </div>
                                    </div>
                                {{/if}}
                                
                                {{#if this.status}}
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <span class="badge bg-success">
                                                <i class="fas fa-check-circle"></i> Activo
                                            </span>
                                        </div>
                                    </div>
                                {{else}}
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <span class="badge bg-secondary">
                                                <i class="fas fa-times-circle"></i> Inactivo
                                            </span>
                                        </div>
                                    </div>
                                {{/if}}
                                
                                {{#if this.thumbnail}}
                                    <div class="row">
                                        <div class="col-12">
                                            <small class="text-muted">
                                                <i class="fas fa-image"></i> <strong>Imagen:</strong> {{this.thumbnail}}
                                            </small>
                                        </div>
                                    </div>
                                {{/if}}
                            </div>
                            <div class="card-footer bg-light">
                                <small class="text-muted">
                                    <i class="fas fa-hashtag"></i> ID: {{this.id}}
                                </small>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
            
            <div class="row mt-4">
                <div class="col-12">
                    <div class="alert alert-light border" role="alert">
                        <div class="row">
                            <div class="col-md-8">
                                <h5 class="alert-heading">
                                    <i class="fas fa-chart-bar"></i> Estadísticas
                                </h5>
                                <p class="mb-0">
                                    <strong>Total de productos:</strong> {{products.length}} | 
                                    <strong>Stock total:</strong> {{#each products}}{{this.stock}}{{#unless @last}} + {{/unless}}{{/each}}
                                </p>
                            </div>
                            <div class="col-md-4 text-end">
                                <a href="/realtimeproducts" class="btn btn-primary">
                                    <i class="fas fa-sync-alt"></i> Ver en Tiempo Real
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {{else}}
            <div class="row">
                <div class="col-12">
                    <div class="alert alert-warning text-center" role="alert">
                        <i class="fas fa-exclamation-triangle fa-3x mb-3"></i>
                        <h4 class="alert-heading">No hay productos disponibles</h4>
                        <p>Parece que no hay productos registrados en el sistema.</p>
                        <hr>
                        <p class="mb-0">
                            <a href="/realtimeproducts" class="btn btn-primary">
                                <i class="fas fa-plus"></i> Agregar productos en tiempo real
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        {{/if}}
    </div>
</div>

<script>
    // Calcular stock total dinámicamente
    document.addEventListener('DOMContentLoaded', function() {
        const products = {{{json products}}};
        if (products && products.length > 0) {
            const totalStock = products.reduce((sum, product) => sum + product.stock, 0);
            console.log('Total de productos:', products.length);
            console.log('Stock total:', totalStock);
        }
    });
</script>